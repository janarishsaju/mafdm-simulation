# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
*.log

# Node / frontend dev
frontend/node_modules/
frontend/.vite/

# Keep frontend/dist/ — it's committed so Render can serve the built UI
# !frontend/dist/

# Local results / experiment outputs (keep results/ for the pre-cached SSE CSVs)
phase_8/
astrazeneca/
__experiments__/

# OS
.DS_Store
Thumbs.db
